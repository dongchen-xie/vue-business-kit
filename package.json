{
  "name": "vue-ep-toolkit",
  "version": "1.0.0",
  "description": "Vue3 + TypeScript + Element Plus + Lodash library",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "lib:build": "pnpm --filter vue-ep-toolkit build",
    "lib:release": "pnpm build:lib && pnpm --filter vue-ep-toolkit publish --registry https://registry.npmjs.org/",
    "docs:dev": "pnpm --filter docs dev",
    "docs:build": "pnpm --filter docs build"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">= 18.0.0",
    "pnpm": ">=10.0.0"
  }
}
